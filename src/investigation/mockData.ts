/**
 * Workbench navigation. Evidence comes from Handy’s investigation session
 * (provenance log), not Steel.
 */
export type WorkbenchNav =
  | "overview"
  | "details"
  | "findings"
  | "evidence"
  | "timeline"
  | "replay";

export type { EvidenceItem } from "@/browser";
